<?xml version="1.0" encoding="utf-8"?>
<credential-provider>
    <capabilities>
        <capability name="androidx.credentials.TYPE_PUBLIC_KEY_CREDENTIAL" />
        <!-- latch: passwords. The passkey capability above is untouched; the
             hardware FIDO2 path is exactly as upstream left it. -->
        <capability name="androidx.credentials.TYPE_PASSWORD_CREDENTIAL" />
    </capabilities>
</credential-provider>
